import ProviderUserSettings from "@/components/super-user/settings-super-user/settings";
import { Grid } from "@mui/system";

const ProviderSettingsPage = () => {
  return (
    <Grid width={"100%"}>
      <ProviderUserSettings />
    </Grid>
  );
};

export default ProviderSettingsPage;
